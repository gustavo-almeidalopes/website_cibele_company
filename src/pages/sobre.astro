---
import BaseLayout from '../layouts/BaseLayout.astro';

const team = [
  {
    nome: 'Gustavo Lopes',
    rgm: '3XX83XX8',
    papel: 'Front-end & Back-end',
    bio: 'Responsável pelas páginas de produtos, carrinho e pela nova camada de front-end em Astro.',
    img: 'gustavo.jpg',
  },
  {
    nome: 'Matheus Souza',
    rgm: '3XX14XX3',
    papel: 'Back-end & QA',
    bio: 'Atuação em back-end, revisão de código e consistência visual entre as páginas.',
    img: 'matheus.webp',
  },
];

const values = [
  { t: 'Origem renovável', d: 'Priorizamos matérias-primas de fontes vegetais em vez de petróleo.' },
  { t: 'Ciclo curto', d: 'Produtos pensados para se decompor rapidamente após o uso.' },
  { t: 'Transparência', d: 'Clareza sobre composição e impacto de cada item do catálogo.' },
];
---

<BaseLayout
  active="sobre"
  title="Sobre — Cibele Plastic"
  description="A história e a equipe por trás da Cibele Plastic, projeto acadêmico de e-commerce de bioplásticos sustentáveis."
>
  <section class="phero" data-hero>
    <div class="container">
      <p class="eyebrow" data-hero-item>Sobre nós</p>
      <h1 class="display phero__title">
        <span class="line-mask"><span class="line-inner">Bioplástico com</span></span>
        <span class="line-mask"><span class="line-inner">propósito.</span></span>
      </h1>
      <p class="lead phero__lead" data-hero-item>
        A Cibele Plastic nasceu como um projeto acadêmico com uma ambição simples: mostrar que é
        possível ter o desempenho do plástico convencional sem o peso ambiental do plástico fóssil.
      </p>
    </div>
  </section>

  <section class="section-lg story paper">
    <div class="container">
      <div class="story__grid">
        <p class="eyebrow" data-reveal="fade">(A história)</p>
        <div class="story__body">
          <h2 class="h-md story__title" data-lines>
            <span class="line-mask"><span class="line-inner">Do laboratório à</span></span>
            <span class="line-mask"><span class="line-inner">mesa, sem culpa.</span></span>
          </h2>
          <p data-reveal="up">
            Escolhemos um foco: embalagens. Copos, pratos, talheres, marmitas e sacolas em
            bioplástico de fontes renováveis, com o mesmo desempenho do plástico convencional — e
            um ciclo de vida que devolve à natureza o que dela veio.
          </p>
          <p data-reveal="up">
            Cada coleção é pensada para um contexto real: da mesa posta ao delivery, do café da
            manhã do hotel ao evento lotado. Sustentabilidade e qualidade, na prática.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="section values">
    <div class="container">
      <div class="values__grid" data-reveal-group>
        {
          values.map((v, i) => (
            <div class="value" data-reveal-item>
              <span class="value__num">0{i + 1}</span>
              <h3 class="value__title">{v.t}</h3>
              <p class="text-muted">{v.d}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section team">
    <div class="container">
      <header class="team__header">
        <p class="eyebrow" data-reveal="fade">A equipe</p>
        <h2 class="h-xl" data-lines>
          <span class="line-mask"><span class="line-inner">Quem faz</span></span>
        </h2>
      </header>

      <div class="team__grid" data-reveal-group>
        {
          team.map((member) => (
            <article class="member" data-reveal-item>
              <div class="member__photo">
                <img src={`/img/${member.img}`} alt={member.nome} loading="lazy" width="600" height="720" />
                <span class="member__role">{member.papel}</span>
              </div>
              <div class="member__info">
                <h3 class="member__name">{member.nome}</h3>
                <p class="member__rgm">RGM {member.rgm}</p>
                <p class="member__bio text-muted">{member.bio}</p>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .phero {
    padding-block: clamp(2rem, 5vw, 3rem) clamp(2rem, 5vw, 4rem);
  }

  .phero .eyebrow {
    margin-bottom: 1.5rem;
  }

  .phero__title {
    margin-bottom: var(--space-32);
  }

  .phero__lead {
    max-width: 56ch;
    color: var(--muted);
  }

  .story__grid {
    display: grid;
    gap: 2rem;
  }

  .story__body {
    display: grid;
    gap: 1.4rem;
    max-width: 60ch;
  }

  .story__title {
    margin-bottom: 0.6rem;
  }

  .story__body p {
    color: var(--muted-light);
    font-size: var(--step-1);
    line-height: 1.5;
  }

  @media (min-width: 900px) {
    .story__grid {
      grid-template-columns: 0.5fr 1.5fr;
      gap: 4rem;
    }
  }

  .values__grid {
    display: grid;
    gap: 1.4rem;
    grid-template-columns: 1fr;
  }

  .value {
    padding: 24px;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: var(--ink-2);
    transition: border-color var(--duration-slow) var(--ease), transform var(--duration-slow) var(--ease);
  }

  .value:hover {
    border-color: var(--line-strong);
    transform: translateY(-4px);
  }

  .value__num {
    display: block;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--accent-ink);
    margin-bottom: 1.4rem;
  }

  .value__title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.3rem;
    margin-bottom: 0.6rem;
  }

  @media (min-width: 760px) {
    .values__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .team__header {
    margin-bottom: 2.5rem;
  }

  .team__header .eyebrow {
    margin-bottom: 1rem;
  }

  .team__grid {
    display: grid;
    gap: 1.6rem;
    grid-template-columns: 1fr;
  }

  .member {
    border: 1px solid var(--line);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--ink-2);
    transition: transform var(--duration-slow) var(--ease), border-color var(--duration-slow) var(--ease);
  }

  .member:hover {
    transform: translateY(-4px);
    border-color: var(--line-strong);
  }

  .member__photo {
    position: relative;
    aspect-ratio: 4 / 3;
    overflow: hidden;
  }

  .member__photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: grayscale(1) contrast(1.05);
    transition: filter var(--duration-slow) var(--ease);
  }

  .member:hover .member__photo img {
    filter: grayscale(0);
  }

  .member__role {
    position: absolute;
    left: 1rem;
    bottom: 1rem;
    padding: 0.4rem 0.9rem;
    border-radius: var(--radius-pill);
    background: var(--forest);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    color: var(--cream);
  }

  .member__info {
    padding: 24px;
  }

  .member__name {
    font-size: 1.5rem;
    margin-bottom: 0.3rem;
  }

  .member__rgm {
    font-size: 0.78rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--muted);
    margin-bottom: 0.9rem;
  }

  .member__bio {
    font-size: 0.92rem;
    max-width: 46ch;
  }

  @media (min-width: 720px) {
    .team__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
